---
title: "Основные концепции"
description: "Основные концепции модуля интеграций: дескриптор, роли и путь параметров от конфигурации до рантайма."
---

# {frontmatter.title}

В этой главе вы узнаете о Модуле интеграций и о том, как его использовать.

## Где можно применять Модуль интеграций

Модуль интеграций можно применять в самых разных сценариях. Он не зависит от типа провайдера, и ему всё равно, для чего нужны параметры.

- **Любой тип провайдера:** Платежи, доставка, уведомления, аналитика, авторизация и другие.
- **Плагины:** Добавляйте плагину настраиваемые параметры, не разрабатывая UI и слой данных.
- **Кастомные модули и расширения:** Любой модуль Medusa, которому нужны настраиваемые параметры в Admin, например API-ключи, режимы или feature-флаги.

Если администратор магазина должен уметь управлять настройкой, её можно сделать через Модуль интеграций.

## Провайдеры и потребители

В интеграции участвуют две роли разработчика, а между ними находится администратор магазина.

- **Провайдер интеграции:** Объявляет интеграцию. Описывает параметры, их группировку и валидацию, а также необязательную проверку соединения. Провайдером интеграции может быть любой плагин, провайдер или кастомный модуль, и всё это он объявляет в одном [дескрипторе](#дескриптор).
- **Администратор магазина:** Настраивает в Admin эти параметры, например учётные данные, режимы или вебхуки.
- **Потребитель:** Читает параметры [инстанса](#инстансы-и-их-идентификаторы) интеграции в рантайме.

Провайдер и потребитель обычно живут в одном пакете. Платёжный плагин объявляет свои учётные данные, затем считывает их обратно в своей платёжной логике. Потребителем может быть и посторонний код, например API-роут, подписчик или запланированная задача, которым нужны параметры настроенной интеграции.

## Дескриптор

Дескриптор – единое объявление метаданных интеграции, её параметров, секций настроек, валидации и необязательной проверки соединения. Создаётся он через `defineIntegration`.

Например:

```ts title="providers/integration-acme/services/acme-integration.ts"
import { defineIntegration, AbstractIntegrationProvider } from "@gorgo/medusa-integration"

const descriptor = defineIntegration({
  category: "payment",
  displayName: "acme.name",
  options: {
    apiKey:  {
      type: "string",
      control: "secret",
      secret: true,
      required: true,
      label: "acme.apiKey"
    },
    sandbox: {
      type: "boolean",
      control: "switch",
      default: false,
      label: "acme.sandbox"
    },
  },
  sections: [
    {
      id: "credentials",
      title: "acme.credentials",
      options: ["apiKey", "sandbox"]
    },
  ],
})

export class AcmeIntegration extends AbstractIntegrationProvider {
  static identifier = "acme"

  get descriptor() {
    return descriptor
  }
}
```

Дескриптор служит единым источником истины для интеграции. По нему модуль генерирует CRUD API, валидирует запись, рендерит UI настроек и определяет, что потребители получают в рантайме. Вам не нужно писать ни модели данных, ни API-роуты, ни UI-формы.


## Инстансы и их идентификаторы

Инстанс – это отдельная регистрация провайдера интеграции с собственной конфигурацией. Один и тот же провайдер можно зарегистрировать несколько раз, и каждая регистрация даёт самостоятельный инстанс со своими настройками в Admin.

Каждый инстанс интеграции адресуется по `provider_id` вида:

```
int_<identifier>[_<instanceId>]
```

У каждой интеграции есть стабильный идентификатор, например `acme`, заданный как `static identifier` в провайдере:

```ts title="providers/integration-acme/services/acme-integration.ts"
import { defineIntegration, AbstractIntegrationProvider } from "@gorgo/medusa-integration"

// ...

export class AcmeIntegration extends AbstractIntegrationProvider {
  static identifier = "acme"

  // ...
}
```


### Один инстанс

Это вариант по умолчанию. У него нет ID инстанса (`instanceId`), поэтому его `provider_id` выглядит, например как `int_acme`.

Например:


```ts title="medusa-config.ts"
module.exports = defineConfig({
  // ...
  plugins: [
    {
      resolve: "@gorgo/medusa-integration",
      options: {
        // ...
        providers: [
          {
            resolve: "@gorgo/medusa-payment-acme/providers/integration-acme",
            options: {},
          },
        ],
      },
    },
  ],
})
```

- **Несколько инстансов:** зарегистрируйте один и тот же провайдер несколько раз, каждый со своим ID. Каждый инстанс настраивается независимо, например для поддержки нескольких аккаунтов интегрируемых сервисов.

Например, две независимые регистрации одного и того же провайдера:

```ts title="medusa-config.ts"
module.exports = defineConfig({
  // ...
  plugins: [
    {
      resolve: "@gorgo/medusa-integration",
      options: {
        // ...
        providers: [
          {
            resolve: "@gorgo/medusa-payment-acme/providers/integration-acme",
            id: "eu",
            options: {},
          },
          {
            resolve: "@gorgo/medusa-payment-acme/providers/integration-acme",
            id: "us",
            options: {},
          },
        ],
      },
    },
  ],
})
```

Это даст `int_acme_eu` и `int_acme_us` соответственно. `id` задаётся на верхнем уровне записи. Если случайно поместить его внутрь `options`, провайдер зарегистрируется под дефолтным (безымянным) инстансом без предупреждения.

Потребитель получает параметры по идентификатору и инстансу, поэтому один и тот же код может обращаться к нужному.

## Параметры интеграции

Параметры интеграции движутся по предсказуемому пути. Автор объявляет их в дескрипторе, администратор настраивает, модуль валидирует, они становятся активными, как только интеграция включена и заполнена, а потребитель получает их в рантайме. По пути проверка соединения может сверить их со сторонним сервисом.

## Поля параметра

Каждый параметр в `options` объект вида `{ type, ... }`. Поле `type` определяет доступные значения и остальные поля, общие для всех типов:

<MedusaTypeList
  sectionTitle="Options"
  types={[
    {
      name: "type",
      type: "enum",
      description: "Тип параметра. Определяет валидацию, доступный UI-контрол по умолчанию и дополнительные поля:\n- `string`\n- `url`\n- `email`\n- `uuid`\n- `number`\n- `boolean`\n- `enum`\n- `json`",
      optional: false,
      expandable: false,
      children: []
    },
    {
      name: "label",
      type: "string",
      description: "i18n-ключ метки поля. Резолвится в Admin по активному языку с откатом на английский.",
      optional: false,
      expandable: false,
      children: []
    },
    {
      name: "control",
      type: "enum",
      description: "UI-контрол для рендера поля:\n- `text`\n- `secret`\n- `switch`\n- `number`\n- `url`\n- `select`\n- `json`\nПодбирается автоматически по `type`, но можно переопределить.",
      optional: true,
      expandable: false,
      children: []
    },
    {
      name: "default",
      type: "string | number | boolean | unknown",
      description: "Значение по умолчанию; тип соответствует `type` параметра. Наличие `default` делает параметр фактически всегда присутствующим в резолвнутых опциях, даже если `required` не задан.",
      optional: true,
      expandable: false,
      children: []
    },
    {
      name: "required",
      type: "boolean",
      description: "Отклоняет пустое (`undefined`) значение и при сохранении этого параметра, и при проверке полноты интеграции. По умолчанию параметр необязателен.",
      optional: true,
      defaultValue: "false",
      expandable: false,
      children: []
    },
    {
      name: "secret",
      type: "boolean",
      description: "Шифрует значение (AES-256-GCM) при сохранении и никогда не передаёт его в браузер. Admin API маскирует значение и сообщает лишь, задано ли оно. Сохранение пустым оставляет прежнее хранимое значение.",
      optional: true,
      defaultValue: "false",
      expandable: false,
      children: []
    },
    {
      name: "hint",
      type: "string",
      description: "i18n-ключ подсказки, отображаемой под полем.",
      optional: true,
      expandable: false,
      children: []
    },
    {
      name: "placeholder",
      type: "string",
      description: "i18n-ключ плейсхолдера поля.",
      optional: true,
      expandable: false,
      children: []
    },
    {
      name: "readonly",
      type: "boolean",
      description: "Рендерит поле как нередактируемое в Admin.",
      optional: true,
      expandable: false,
      children: []
    },
    {
      name: "visibleWhen",
      type: "object",
      description: "`{ field, equals }` скрывает поле в Admin, пока значение соседнего параметра `field` не равно `equals`. Влияет только на отображение: требуемость на сервере по-прежнему задаётся через `required` или `validate`.",
      optional: true,
      expandable: false,
      children: []
    },
    {
      name: "validate",
      type: "function",
      description: "Кастомная проверка значения этого параметра, вызывается вместе со встроенной схемой типа.",
      optional: true,
      expandable: false,
      children: []
    }
  ]}
/>

## Дополнительные поля по типу параметра

У некоторых типов есть свои поля сверх общих:

- **`string`:** `minLength`, `maxLength`, `pattern`
- **`url`:** `protocols`
- **`number`:** `min`, `max`, `int`, `positive`, `nonnegative`, `multipleOf`
- **`enum`:** `values`, `valueLabels`

<MedusaTypeList
  sectionTitle="Options"
  types={[
    {
      name: "minLength",
      type: "number",
      description: "Минимальная длина строки.\nПрименимо только при `type: \"string\"`.",
      optional: true,
      expandable: false,
      children: []
    },
    {
      name: "maxLength",
      type: "number",
      description: "Максимальная длина строки.\nПрименимо только при `type: \"string\"`.",
      optional: true,
      expandable: false,
      children: []
    },
    {
      name: "pattern",
      type: "string",
      description: "Regex-шаблон для валидации строки.\nПрименимо только при `type: \"string\"`.",
      optional: true,
      expandable: false,
      children: []
    },
    {
      name: "protocols",
      type: "string[]",
      description: "Ограничивает допустимые схемы URL, например `[\"https\"]`. Без ограничения разрешён любой протокол.\nПрименимо только при `type: \"url\"`.",
      optional: true,
      expandable: false,
      children: []
    },
    {
      name: "min",
      type: "number",
      description: "Минимальное значение.\nПрименимо только при `type: \"number\"`.",
      optional: true,
      expandable: false,
      children: []
    },
    {
      name: "max",
      type: "number",
      description: "Максимальное значение.\nПрименимо только при `type: \"number\"`.",
      optional: true,
      expandable: false,
      children: []
    },
    {
      name: "int",
      type: "boolean",
      description: "Требует целое число.\nПрименимо только при `type: \"number\"`.",
      optional: true,
      expandable: false,
      children: []
    },
    {
      name: "positive",
      type: "boolean",
      description: "Требует значение больше нуля.\nПрименимо только при `type: \"number\"`.",
      optional: true,
      expandable: false,
      children: []
    },
    {
      name: "nonnegative",
      type: "boolean",
      description: "Требует значение не меньше нуля.\nПрименимо только при `type: \"number\"`.",
      optional: true,
      expandable: false,
      children: []
    },
    {
      name: "multipleOf",
      type: "number",
      description: "Требует, чтобы значение было кратно указанному числу.\nПрименимо только при `type: \"number\"`.",
      optional: true,
      expandable: false,
      children: []
    },
    {
      name: "values",
      type: "string[]",
      description: "Список допустимых значений. Обязательное поле.\nПрименимо только при `type: \"enum\"`.",
      optional: false,
      expandable: false,
      children: []
    },
    {
      name: "valueLabels",
      type: "object",
      description: "i18n-ключи меток для отдельных значений из `values`.\nПрименимо только при `type: \"enum\"`.",
      optional: true,
      expandable: false,
      children: []
    }
  ]}
/>

### Сгенерированный CRUD и валидация

Модуль генерирует admin CRUD API из дескриптора и валидирует каждую запись. Валидация покрывает правила на уровне отдельного параметра (типы, диапазоны, паттерны) вместе с правилами между секциями, охватывающими всю конфигурацию. Ничего писать под каждую интеграцию не нужно.

Правила между секциями объявляются через `validate` дескриптора. В отличие от `validate` отдельного параметра, он получает весь собранный конфиг и может пометить проблемой любое поле:

```ts title="providers/integration-acme/services/acme-integration.ts"
const descriptor = defineIntegration({
  // ...
  validate: (full, { addIssue }) => {
    if (full.mode === "webhook" && !full.webhookSecret) {
      addIssue({ path: ["webhookSecret"], message: "Required when mode is \"webhook\"" })
    }
  },
})
```

В отличие от `required` или `validate` конкретного параметра, это правило запускается только при полной валидации (проверке готовности интеграции к включению), а не при сохранении отдельной секции.

### Включена и заполнена

Параметры становятся активными, только когда интеграция и включена, и заполнена, то есть проходит полную валидацию. Незавершённый черновик или выключенная интеграция не резолвятся никогда, поэтому недоделанная конфигурация не может утечь в рантайм.

### Секреты и шифрование

Параметры с пометкой `secret` шифруются при хранении алгоритмом AES-256-GCM и никогда не попадают в браузер. Admin API маскирует их и сообщает лишь, задано значение или нет. Сохранение секрета пустым оставляет прежнее хранимое значение, а не стирает его.

Для шифрования модулю нужен ключ. Задайте его как `encryptionKey` при регистрации модуля в `medusa-config.ts`:

```ts title="medusa-config.ts"
module.exports = defineConfig({
  // ...
  plugins: [
    {
      resolve: "@gorgo/medusa-integration",
      options: {
        encryptionKey: process.env.INTEGRATION_ENCRYPTION_KEY,
        providers: [
          // ...
        ],
      },
    },
  ],
})
```

```text title=".env"
INTEGRATION_ENCRYPTION_KEY=supersecret
```

Ключом может быть любая непустая строка, но для продакшена подойдёт высокоэнтропийное значение, например `openssl rand -hex 32`. Без него модуль выбрасывает ошибку при первой попытке сохранить или прочитать интеграцию, у которой есть хотя бы один параметр с `secret: true`.

### Проверка соединения

Дескриптор может объявить необязательную проверку соединения, которая сверяет учётные данные со сторонним сервисом. Администраторы запускают её по требованию, а запланированная задача периодически перепроверяет настроенные интеграции.

Добавьте `testConnection` в дескриптор. Он получает уже резолвнутые (расшифрованные) параметры и возвращает статус:

```ts title="providers/integration-acme/services/acme-integration.ts"
const descriptor = defineIntegration({
  // ...
  testConnection: async ({ options }) => {
    const res = await fetch("https://api.acme.com/ping", {
      headers: { Authorization: `Bearer ${options.apiKey}` },
    })

    if (!res.ok) {
      return { status: "failed", message: `Acme ответил ${res.status}` }
    }

    return { status: "passed" }
  },
})
```

`status` принимает одно из значений: `passed`, `failed` или `skipped`; необязательное `message` отображается в Admin рядом с результатом проверки.

### Резолв параметров в рантайме

Потребители читают типизированные, провалидированные и расшифрованные параметры с применёнными значениями по умолчанию из дескриптора. Незаполненная или выключенная интеграция резолвится в ничто, а не в частичные данные. Резолвнутые параметры кэшируются ненадолго и обновляются при каждом изменении конфигурации.

## Генерация Admin UI

Модуль генерирует UI интеграции из её дескриптора, поэтому строить страницы не нужно.

### Секции

Параметры группируются в секции настроек и рендерятся через [LayoutComposer](https://docs.medusajs.com/resources/admin-components/components/layout-composer) Medusa в виде карточек с изменяемым порядком, в одну или две колонки.

### Кастомные секции

Когда сгенерированного Admin UI недостаточно, вы можете построить для своих параметров любой интерфейс на кастомных admin-виджетах. Это обычные admin-виджеты Medusa на том же механизме `defineWidgetConfig` и зон внедрения, что вы уже используете в других местах, так что учить ничего нового не придётся. Модуль предоставляет зоны внедрения на странице каждой интеграции (например, `gorgo.integration.<identifier>.main.after`), и виджет, нацеленный на такую зону, получает готовый `providerId` и напрямую читает и пишет параметры интеграции.

### Локализация

Метки, подсказки и заголовки в дескрипторе задаются как i18n-ключи. Поставляйте переводы вместе со своей интеграцией, и UI настроек локализуется автоматически.

Всё это построено на стандартном admin i18n Medusa, так что учить ничего нового не нужно. Регистрируйте переводы привычным способом, и ключи резолвятся по активному языку Admin с откатом на английский, если ключ отсутствует. Кастомные виджеты локализуются так же, используя тот же каталог сообщений, что и сгенерированная форма.

## Дальнейшие шаги

- [Чтение параметров](/medusa-modules/integration/reading-options)
- [Перенос провайдера на модуль интеграций](/medusa-modules/integration/migrate-provider-to-integration-module)
- [Руководство администратора](/medusa-modules/integration/manage-integrations)
