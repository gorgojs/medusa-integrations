---
title: "Интеграция Т-Касса в витрину магазина"
description: "Узнайте, как интегрировать платёжный провайдер Т-Касса от Т-Банка в storefront (витрину магазина) на Next.js для Medusa."
---

# {frontmatter.title}

Для интеграции платёжного провайдера Т-Касса от Т-Банка в storefront [Medusa DTC Starter от Gorgo](https://github.com/gorgojs/medusa-dtc-starter) начните с добавления необходимых UI-компонентов. Таким образом провайдер будет отображаться на странице оформления заказа наряду с другими доступными методами оплаты.

Когда покупатель выбирает Т-Касса, оформление заказа инициализирует платёжную сессию, а провайдер создаёт её на своей стороне и возвращает адрес для оплаты. Кнопка оплаты отправляет покупателя на эту страницу, где он может заплатить картой или через СБП.

После завершения оплаты Т-Касса одновременно отправляет вебхук и перенаправляет покупателя обратно в витрину.

Для витрины на Next.js необходимо внести следующие изменения:

## Шаг 1: Настройте платёжный провайдер

Откройте [`src/lib/constants.tsx`](https://github.com/gorgojs/medusa-integrations/blob/4db4c78b5af5a75285b9ed658192b6c6c818eeb6/examples/payment-tkassa/apps/storefront/src/lib/constants.tsx#L56-L60) и добавьте Т-Касса в маппинг платёжных провайдеров, а также вспомогательную функцию, распознающую её сессии:

![Структура проекта Medusa Storefront после обновления файла с константами](/static/tkassa/storefront/src-lib-constants.webp)

```ts title="src/lib/constants.tsx"
export type PaymentInfo = {
  title: string
  subtitle?: string
  icon: React.JSX.Element
}

export const paymentInfoMap: Record<string, PaymentInfo> = {
  // ...
  pp_tkassa_tkassa: {
    title: "T-Kassa",
    subtitle: "Cards, SBP",
    icon: <TKassa />,
  },
  // ...
}

// ...

export const isTkassa = (providerId?: string) => {
  return providerId?.startsWith("pp_tkassa")
}
```

`title` называет способ оплаты на странице оформления заказа, а `subtitle` задаёт строку помельче под ним, куда попадают способы оплаты, которые принимает Т-Касса. Оба служат резервными значениями, когда для локали нет перевода. `isTkassa` использует диспетчер ниже и построитель данных платёжной сессии из следующего шага.

Компонент для `icon` вы добавляете сами. В примере щит Т-Касса нарисован фирменным жёлтым с тёмной буквой Т, а `viewBox` обрезан по самому знаку, чтобы тот встал по центру круга 24px, который рисует вокруг него страница оформления заказа:

```tsx title="src/modules/common/icons/tkassa.tsx"
import type React from "react"

import type { IconProps } from "types/icon"

const TKassa: React.FC<IconProps> = ({
  size = "16",
  color: _color,
  ...attributes
}) => {
  return (
    <svg
      width={size}
      height={size}
      viewBox="50 60 200 200"
      role="img"
      fill="none"
      xmlns="http://www.w3.org/2000/svg"
      {...attributes}
    >
      <title>T-Kassa icon</title>
      <path
        d="M50 60H250V160.313C250 186.028 236.244 209.791 213.914 222.651L150 259.457L86.0859 222.651C63.7557 209.791 50 186.028 50 160.313V60Z"
        fill="#FFDD2D"
      />
      <path
        fillRule="evenodd"
        clipRule="evenodd"
        d="M104.76 112.797V143.996C109.038 139.178 116.816 135.918 125.709 135.918H135.374V172.187C135.374 181.836 132.745 190.282 128.844 194.927H170.883C166.99 190.277 164.368 181.842 164.368 172.205V135.918H174.033C182.926 135.918 190.704 139.178 194.982 143.996V112.797H104.76Z"
        fill="#333333"
      />
    </svg>
  )
}

export default TKassa
```

---

## Шаг 2: Инициализируйте платёжную сессию

Откройте тот же файл [`src/lib/constants.tsx`](https://github.com/gorgojs/medusa-integrations/blob/4db4c78b5af5a75285b9ed658192b6c6c818eeb6/examples/payment-tkassa/apps/storefront/src/lib/constants.tsx#L76-L108) и добавьте адрес возврата и построитель данных сессии:

![Структура проекта Medusa Storefront после обновления файла с константами](/static/tkassa/storefront/src-lib-constants.webp)

```ts title="src/lib/constants.tsx"
/**
 * Адрес, на который покупатель возвращается после оплаты на стороне провайдера.
 * `<html lang>` хранит локаль, в которой отображалось оформление заказа, поэтому
 * покупатель возвращается на том языке, на котором ушёл из магазина.
 */
const paymentReturnUrl = (providerId: string, cartId: string) => {
  const origin =
    typeof window === "undefined" ? getBaseURL() : window.location.origin
  const locale =
    typeof document === "undefined"
      ? defaultLocale
      : document.documentElement.lang || defaultLocale

  return `${origin}/api/payment-return?provider=${providerId}&cart_id=${cartId}&locale=${locale}`
}

const paymentSessionDataBuilders: Array<{
  test: (providerId?: string) => boolean | undefined
  isReady?: (cart: HttpTypes.StoreCart) => boolean
  build: (cart: HttpTypes.StoreCart) => Record<string, unknown>
}> = [
  {
    test: (providerId) => isTkassa(providerId),
    // Т-Касса формирует чек из корзины, а чеку нужны email и телефон
    // покупателя. Инициализация сессии до того, как оформление заказа собрало
    // эти данные, создаёт платёж без чека.
    isReady: (cart) => Boolean(cart.email && cart.shipping_address),
    build: (cart) => {
      const returnUrl = paymentReturnUrl("tkassa", cart.id)

      return {
        SuccessURL: returnUrl,
        FailURL: returnUrl,
        cart,
      }
    },
  },
]
```

При инициализации платежа для Т-Касса `build` передаёт `SuccessURL` и `FailURL`, оба указывающие на один и тот же адрес возврата, а также `cart` для формирования чека. `isReady` удерживает сессию, пока в корзине не появятся email и адрес доставки, поскольку для чека нужны email и телефон покупателя.

---

## Шаг 3: Создайте кнопку оплаты

Создайте файл [`src/modules/checkout/components/payment-button/providers/tkassa.tsx`](https://github.com/gorgojs/medusa-integrations/blob/350802b8704d370bd65f634f1934bd1b642e7ab3/examples/payment-tkassa/apps/storefront/src/modules/checkout/components/payment-button/providers/tkassa.tsx):

![Структура проекта Medusa Storefront после создания файла для кнопки оплаты Т-Касса](/static/tkassa/storefront/src-modules-checkout-components-payment-button-providers-tkassa.webp)

```ts title="src/modules/checkout/components/payment-button/providers/tkassa.tsx"
"use client"

import type { HttpTypes } from "@medusajs/types"
import { useTranslations } from "next-intl"
import { useState } from "react"
import { PaymentButtonShell, type PaymentProviderButtonProps } from "../shared"

const paymentUrl = (cart: HttpTypes.StoreCart) => {
  const session = cart.payment_collection?.payment_sessions?.find((s) =>
    s.provider_id.startsWith("pp_tkassa")
  )

  return (session?.data as { PaymentURL?: string } | undefined)?.PaymentURL
}

/**
 * Т-Касса принимает оплату на собственной странице. Сессия, созданная оформлением
 * заказа, хранит адрес, куда отправить покупателя, а заказ оформляется, когда он
 * вернётся через `/api/payment-return`, либо по вебхуку, если тот придёт раньше.
 */
const TkassaPaymentButton = ({
  cart,
  notReady,
  label,
  cartUpdating,
  "data-testid": dataTestId,
}: PaymentProviderButtonProps) => {
  const t = useTranslations("Errors")
  const [submitting, setSubmitting] = useState(false)
  const [errorMessage, setErrorMessage] = useState<string | null>(null)

  const handlePayment = () => {
    const url = paymentUrl(cart)

    if (!url) {
      setErrorMessage(t("paymentFailed"))
      return
    }

    setSubmitting(true)
    window.location.assign(url)
  }

  return (
    <PaymentButtonShell
      label={label}
      onClick={handlePayment}
      disabled={notReady}
      loading={submitting || cartUpdating}
      errorMessage={errorMessage}
      errorTestId="tkassa-payment-error-message"
      data-testid={dataTestId ?? "submit-order-button"}
    />
  )
}

export default TkassaPaymentButton
```

Этот компонент находит `payment_session` Т-Касса в активной корзине и извлекает `data.PaymentURL`. При нажатии `handlePayment` отправляет покупателя туда напрямую через `window.location.assign`, поскольку страница Т-Касса не встраивается в витрину.

Если `PaymentURL` отсутствует, кнопка показывает сообщение об ошибке вместо перехода.

---

## Шаг 4: Зарегистрируйте кнопку оплаты

Импортируйте кнопку и `isTkassa` в [`src/modules/checkout/components/payment-button/index.tsx`](https://github.com/gorgojs/medusa-integrations/blob/350802b8704d370bd65f634f1934bd1b642e7ab3/examples/payment-tkassa/apps/storefront/src/modules/checkout/components/payment-button/index.tsx#L3-L17), затем добавьте `case` для неё в `switch` диспетчера:

![Структура проекта Medusa Storefront после обновления файла для компонента кнопки оплаты](/static/tkassa/storefront/src-modules-checkout-components-payment-button-index.webp)

```ts title="src/modules/checkout/components/payment-button/index.tsx"
import {
  isManual,
  isPaymentSessionReady,
  isStripeLike,
  isTkassa,
} from "@lib/constants"
// ...
import TkassaPaymentButton from "./providers/tkassa"

// ...

  switch (true) {
    // ...
    case isTkassa(activePaymentMethod):
      return <TkassaPaymentButton {...providerProps} />
    default:
      // ...
  }
```

Родительский компонент `PaymentButton` использует `isTkassa`, чтобы определить, нужно ли отрисовать `TkassaPaymentButton` для активной сессии, иначе выполнение переходит к следующей ветке.

---

## Шаг 5: Обработайте возврат с оплаты

Т-Касса сообщает статус платежа и через вебхук, и через этот редирект, поэтому общий роут проверяет, есть ли у корзины заказ, прежде чем оформлять его сам.

Откройте [`src/app/api/payment-return/route.ts`](https://github.com/gorgojs/medusa-integrations/blob/350802b8704d370bd65f634f1934bd1b642e7ab3/examples/payment-tkassa/apps/storefront/src/app/api/payment-return/route.ts#L29-L80) и добавьте следующую функцию:

![Структура проекта Medusa Storefront после создания файла для API route](/static/tkassa/storefront/src-app-api-payment-return-route.webp)

```ts title="src/app/api/payment-return/route.ts"
type ReturnContext = {
  cartId: string
  locale: string
  origin: string
}

/**
 * Обрабатывает возврат от провайдера, который принимает оплату на собственной
 * странице и возвращает покупателя только с идентификатором корзины — Т-Касса
 * в их числе.
 *
 * Вебхук провайдера и этот редирект соревнуются друг с другом, и какой из них
 * ни пришёл бы первым, он оформляет заказ, поэтому перед оформлением
 * проверяется, нет ли у корзины уже заказа.
 */
async function redirectProviderReturn({
  cartId,
  locale,
  origin,
}: ReturnContext) {
  const cart = await sdk.client
    .fetch<HttpTypes.StoreCartResponse>(`/store/carts/${cartId}`, {
      method: "GET",
      query: { fields: "id,order_link.order_id" },
      headers: { ...(await getAuthHeaders()) },
      cache: "no-store",
    })
    .then(({ cart }) => cart)
    .catch(() => null)

  if (!cart) {
    return NextResponse.redirect(
      `${origin}/${locale}/cart?error=payment_failed`
    )
  }

  const orderId = (cart as unknown as { order_link?: { order_id?: string } })
    .order_link?.order_id

  if (orderId) {
    const orderCacheTag = await getCacheTag("orders")
    revalidateTag(orderCacheTag)
    await removeCartId()

    return NextResponse.redirect(
      `${origin}/${locale}/order/${orderId}/confirmed`
    )
  }

  // Покупатель отменил оплату, либо она была отклонена, поэтому он возвращается
  // на страницу оформления заказа с нетронутой корзиной и может выбрать способ
  // оплаты заново.
  await setCartId(cartId)

  try {
    await placeOrder(cartId)
  } catch (error) {
    unstable_rethrow(error)

    return NextResponse.redirect(
      `${origin}/${locale}/checkout?error=payment_failed`
    )
  }

  return NextResponse.redirect(
    `${origin}/${locale}/checkout?error=payment_failed`
  )
}
```

Затем направьте возврат Т-Касса в эту функцию прямо в `GET`:

```ts title="src/app/api/payment-return/route.ts"
  if (provider === "tkassa") {
    return redirectProviderReturn({ cartId, locale, origin })
  }
```

Если у корзины уже есть заказ, в гонке победил вебхук, и роут обновляет кэш заказа и отправляет покупателя на страницу подтверждения. Иначе он пытается оформить заказ сам, а при любой ошибке отправляет покупателя обратно на оформление заказа с `error=payment_failed`, чтобы он мог выбрать способ оплаты заново.

---

## Шаг 6: Добавьте подписи провайдера в локали витрины

Добавьте `pp_tkassa_tkassa` в [`messages/en.json`](https://github.com/gorgojs/medusa-integrations/blob/5c7c2f15ccfb90f15d5d94abe7d303137550a421/examples/payment-tkassa/apps/storefront/messages/en.json#L529) и в каждую другую локаль под `messages/`:

```json title="messages/en.json"
{
  "PaymentMethods": {
    // ...
    "pp_tkassa_tkassa": {
      "title": "T-Kassa",
      "subtitle": "Cards, SBP"
    }
  }
}
```

Если для локали нет записи, используются английские `title` и `subtitle` из `paymentInfoMap` в [Шаге 1](#шаг-1-настройте-платёжный-провайдер), а способ оплаты без подписи отрисовывается одним названием. Собственное название Т-Касса не меняется от локали к локали, за исключением русской, где это `Т-Касса`. Подпись переводится наравне с любой другой строкой, поэтому в русской локали она читается как `Карты, СБП`.

---

## Полный пример кода интеграции в витрину

Полный код витрины лежит в [`examples/payment-tkassa/apps/storefront`](https://github.com/gorgojs/medusa-integrations/tree/main/examples/payment-tkassa/apps/storefront), собранной на основе [Medusa DTC Starter от Gorgo](https://github.com/gorgojs/medusa-dtc-starter). В [README](https://github.com/gorgojs/medusa-integrations/blob/main/examples/payment-tkassa/README.md#what-the-example-adds-to-the-starter) примера перечислены все девять файлов, из которых состоит интеграция, включая бэкенд.
