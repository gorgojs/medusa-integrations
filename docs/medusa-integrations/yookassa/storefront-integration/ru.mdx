---
title: "Интеграция ЮKassa в витрину магазина"
description: "Узнайте, как интегрировать платежный провайдер ЮKassa в storefront (витрину магазина) на Next.js, построенную с использованием Medusa."
---

# {frontmatter.title}

Для интеграции платёжного провайдера ЮKassa в storefront [Medusa DTC Starter от Gorgo](https://github.com/gorgojs/medusa-dtc-starter) начните с добавления необходимых UI-компонентов. Таким образом провайдер будет отображаться на странице оформления заказа наряду с другими доступными методами оплаты.

Когда покупатель выбирает ЮKassa, оформление заказа инициализирует платёжную сессию, а провайдер создаёт её на стороне ЮKassa и возвращает адрес для оплаты. Кнопка оплаты отправляет покупателя на эту страницу, где он может выбрать удобный способ оплаты.

После завершения оплаты ЮKassa одновременно отправляет вебхук и перенаправляет покупателя обратно в витрину.

Для витрины на Next.js необходимо внести следующие изменения:

## Шаг 1: Настройте платёжный провайдер

Откройте [`src/lib/constants.tsx`](https://github.com/gorgojs/medusa-integrations/blob/4db4c78b5af5a75285b9ed658192b6c6c818eeb6/examples/payment-yookassa/apps/storefront/src/lib/constants.tsx#L56-L60) и добавьте ЮKassa в маппинг платёжных провайдеров, а также вспомогательную функцию, распознающую её сессии:

![Структура проекта Medusa Storefront после обновления файла с константами](/static/yookassa/storefront/src-lib-constants.webp)

```ts title="src/lib/constants.tsx"
export type PaymentInfo = {
  title: string
  subtitle?: string
  icon: React.JSX.Element
}

export const paymentInfoMap: Record<string, PaymentInfo> = {
  // ...
  pp_yookassa_yookassa: {
    title: "YooKassa",
    subtitle: "Cards, SBP",
    icon: <YooKassa />,
  },
  // ...
}

// ...

export const isYookassa = (providerId?: string) => {
  return providerId?.startsWith("pp_yookassa")
}
```

`title` называет способ оплаты на странице оформления заказа, а `subtitle` задаёт строку помельче под ним, куда попадают способы оплаты, которые ЮKassa собирает на своей странице. Оба служат резервными значениями, когда для локали нет перевода. `isYookassa` использует диспетчер ниже и построитель данных платёжной сессии из следующего шага.

Компонент для `icon` вы добавляете сами. В примере знак ЮKassa нарисован фирменным синим, а `viewBox` обрезан по самому знаку, чтобы тот встал по центру круга 24px, который рисует вокруг него страница оформления заказа:

```tsx title="src/modules/common/icons/yookassa.tsx"
import type React from "react"

import type { IconProps } from "types/icon"

const YooKassa: React.FC<IconProps> = ({
  size = "16",
  color: _color,
  ...attributes
}) => {
  return (
    <svg
      width={size}
      height={size}
      viewBox="73 129 366 254"
      role="img"
      fill="none"
      xmlns="http://www.w3.org/2000/svg"
      {...attributes}
    >
      <title>YooKassa icon</title>
      <path
        fillRule="evenodd"
        clipRule="evenodd"
        d="M178.511 255.527C178.701 185.824 236.332 129 308.751 129C380.466 129 ..."
        fill="#0070F0"
      />
    </svg>
  )
}

export default YooKassa
```

---

## Шаг 2: Инициализируйте платёжную сессию

Откройте тот же файл [`src/lib/constants.tsx`](https://github.com/gorgojs/medusa-integrations/blob/a9a6c9ca380bf9d7807d71bfcdf85da082fafa87/examples/payment-yookassa/apps/storefront/src/lib/constants.tsx#L60-L90) и добавьте адрес возврата и построитель данных сессии:

![Структура проекта Medusa Storefront после обновления файла с константами](/static/yookassa/storefront/src-lib-constants.webp)

```ts title="src/lib/constants.tsx"
/**
 * Адрес, на который покупатель возвращается после оплаты на стороне провайдера.
 * `<html lang>` хранит локаль, в которой отображалось оформление заказа, поэтому
 * покупатель возвращается на том языке, на котором ушёл из магазина.
 */
const paymentReturnUrl = (providerId: string, cartId: string) => {
  const origin =
    typeof window === "undefined" ? getBaseURL() : window.location.origin
  const locale =
    typeof document === "undefined"
      ? defaultLocale
      : document.documentElement.lang || defaultLocale

  return `${origin}/api/payment-return?provider=${providerId}&cart_id=${cartId}&locale=${locale}`
}

const paymentSessionDataBuilders: Array<{
  test: (providerId?: string) => boolean | undefined
  isReady?: (cart: HttpTypes.StoreCart) => boolean
  build: (cart: HttpTypes.StoreCart) => Record<string, unknown>
}> = [
  {
    test: (providerId) => isYookassa(providerId),
    // ЮKassa формирует чек из корзины, а чеку нужны имя, email и телефон
    // покупателя. Инициализация сессии до того, как оформление заказа собрало
    // эти данные, создаёт платёж без чека.
    isReady: (cart) => Boolean(cart.email && cart.shipping_address),
    build: (cart) => ({
      confirmation: {
        type: "redirect",
        return_url: paymentReturnUrl("yookassa", cart.id),
      },
      cart,
    }),
  },
]
```

При инициализации платежа для ЮKassa `build` передаёт объект `confirmation` с `type: "redirect"` и `return_url`, а также `cart` для формирования чека. `isReady` удерживает сессию, пока в корзине не появятся email и адрес доставки.

---

## Шаг 3: Создайте кнопку оплаты

Создайте файл [`src/modules/checkout/components/payment-button/providers/yookassa.tsx`](https://github.com/gorgojs/medusa-integrations/blob/a9a6c9ca380bf9d7807d71bfcdf85da082fafa87/examples/payment-yookassa/apps/storefront/src/modules/checkout/components/payment-button/providers/yookassa.tsx):

![Структура проекта Medusa Storefront после создания файла для кнопки оплаты ЮKassa](/static/yookassa/storefront/src-modules-checkout-components-payment-button-providers-yookassa.webp)

```ts title="src/modules/checkout/components/payment-button/providers/yookassa.tsx"
"use client"

import type { HttpTypes } from "@medusajs/types"
import { useTranslations } from "next-intl"
import { useState } from "react"
import { PaymentButtonShell, type PaymentProviderButtonProps } from "../shared"

const confirmationUrl = (cart: HttpTypes.StoreCart) => {
  const session = cart.payment_collection?.payment_sessions?.find((s) =>
    s.provider_id.startsWith("pp_yookassa")
  )
  const confirmation = session?.data?.confirmation as
    | { confirmation_url?: string }
    | undefined

  return confirmation?.confirmation_url
}

/**
 * ЮKassa принимает оплату на собственной странице. Сессия, созданная оформлением
 * заказа, хранит адрес, куда отправить покупателя, а заказ оформляется, когда он
 * вернётся через `/api/payment-return`, либо по вебхуку, если тот придёт раньше.
 */
const YookassaPaymentButton = ({
  cart,
  notReady,
  label,
  cartUpdating,
  "data-testid": dataTestId,
}: PaymentProviderButtonProps) => {
  const t = useTranslations("Errors")
  const [submitting, setSubmitting] = useState(false)
  const [errorMessage, setErrorMessage] = useState<string | null>(null)

  const handlePayment = () => {
    const url = confirmationUrl(cart)

    if (!url) {
      setErrorMessage(t("paymentFailed"))
      return
    }

    setSubmitting(true)
    window.location.assign(url)
  }

  return (
    <PaymentButtonShell
      label={label}
      onClick={handlePayment}
      disabled={notReady}
      loading={submitting || cartUpdating}
      errorMessage={errorMessage}
      errorTestId="yookassa-payment-error-message"
      data-testid={dataTestId ?? "submit-order-button"}
    />
  )
}

export default YookassaPaymentButton
```

Этот компонент находит `payment_session` ЮKassa в активной корзине и извлекает `data.confirmation.confirmation_url`. При нажатии `handlePayment` отправляет покупателя туда напрямую через `window.location.assign`, поскольку страница ЮKassa не встраивается в витрину.

Если `confirmation_url` отсутствует, кнопка показывает сообщение об ошибке вместо перехода.

---

## Шаг 4: Зарегистрируйте кнопку оплаты

Импортируйте кнопку и `isYookassa` в [`src/modules/checkout/components/payment-button/index.tsx`](https://github.com/gorgojs/medusa-integrations/blob/a9a6c9ca380bf9d7807d71bfcdf85da082fafa87/examples/payment-yookassa/apps/storefront/src/modules/checkout/components/payment-button/index.tsx#L3-L17), затем добавьте `case` для неё в `switch` диспетчера:

![Структура проекта Medusa Storefront после обновления файла для компонента кнопки оплаты](/static/yookassa/storefront/src-modules-checkout-components-payment-button-index.webp)

```ts title="src/modules/checkout/components/payment-button/index.tsx"
import {
  isManual,
  isPaymentSessionReady,
  isStripeLike,
  isYookassa,
} from "@lib/constants"
// ...
import YookassaPaymentButton from "./providers/yookassa"

// ...

  switch (true) {
    // ...
    case isYookassa(activePaymentMethod):
      return <YookassaPaymentButton {...providerProps} />
    default:
      // ...
  }
```

Родительский компонент `PaymentButton` использует `isYookassa`, чтобы определить, нужно ли отрисовать `YookassaPaymentButton` для активной сессии, иначе выполнение переходит к следующей ветке.

---

## Шаг 5: Обработайте возврат с оплаты

ЮKassa сообщает статус платежа и через вебхук, и через этот редирект, поэтому общий роут проверяет, есть ли у корзины заказ, прежде чем оформлять его сам.

Откройте [`src/app/api/payment-return/route.ts`](https://github.com/gorgojs/medusa-integrations/blob/a9a6c9ca380bf9d7807d71bfcdf85da082fafa87/examples/payment-yookassa/apps/storefront/src/app/api/payment-return/route.ts#L29-L80) и добавьте следующую функцию:

![Структура проекта Medusa Storefront после создания файла для API route](/static/yookassa/storefront/src-app-api-payment-return-route.webp)

```ts title="src/app/api/payment-return/route.ts"
type ReturnContext = {
  cartId: string
  locale: string
  origin: string
}

/**
 * Обрабатывает возврат от провайдера, который принимает оплату на собственной
 * странице и возвращает покупателя только с идентификатором корзины — ЮKassa
 * в их числе.
 *
 * Вебхук провайдера и этот редирект соревнуются друг с другом, и какой из них
 * ни пришёл бы первым, он оформляет заказ, поэтому перед оформлением
 * проверяется, нет ли у корзины уже заказа.
 */
async function redirectProviderReturn({
  cartId,
  locale,
  origin,
}: ReturnContext) {
  const cart = await sdk.client
    .fetch<HttpTypes.StoreCartResponse>(`/store/carts/${cartId}`, {
      method: "GET",
      query: { fields: "id,order_link.order_id" },
      headers: { ...(await getAuthHeaders()) },
      cache: "no-store",
    })
    .then(({ cart }) => cart)
    .catch(() => null)

  if (!cart) {
    return NextResponse.redirect(
      `${origin}/${locale}/cart?error=payment_failed`
    )
  }

  const orderId = (cart as unknown as { order_link?: { order_id?: string } })
    .order_link?.order_id

  if (orderId) {
    const orderCacheTag = await getCacheTag("orders")
    revalidateTag(orderCacheTag)
    await removeCartId()

    return NextResponse.redirect(
      `${origin}/${locale}/order/${orderId}/confirmed`
    )
  }

  // Покупатель отменил оплату, либо она была отклонена, поэтому он возвращается
  // на страницу оформления заказа с нетронутой корзиной и может выбрать способ
  // оплаты заново.
  await setCartId(cartId)

  try {
    await placeOrder(cartId)
  } catch (error) {
    unstable_rethrow(error)

    return NextResponse.redirect(
      `${origin}/${locale}/checkout?error=payment_failed`
    )
  }

  return NextResponse.redirect(
    `${origin}/${locale}/checkout?error=payment_failed`
  )
}
```

Затем направьте возврат ЮKassa в эту функцию прямо в `GET`:

```ts title="src/app/api/payment-return/route.ts"
  if (provider === "yookassa") {
    return redirectProviderReturn({ cartId, locale, origin })
  }
```

Если у корзины уже есть заказ, в гонке победил вебхук, и роут обновляет кэш заказа и отправляет покупателя на страницу подтверждения. Иначе он пытается оформить заказ сам, а при любой ошибке отправляет покупателя обратно на оформление заказа с `error=payment_failed`, чтобы он мог выбрать способ оплаты заново.

---

## Шаг 6: Добавьте подписи провайдера в локали витрины

Добавьте `pp_yookassa_yookassa` в [`messages/en.json`](https://github.com/gorgojs/medusa-integrations/blob/5c7c2f15ccfb90f15d5d94abe7d303137550a421/examples/payment-yookassa/apps/storefront/messages/en.json#L529) и в каждую другую локаль под `messages/`:

```json title="messages/en.json"
{
  "PaymentMethods": {
    // ...
    "pp_yookassa_yookassa": {
      "title": "YooKassa",
      "subtitle": "Cards, SBP"
    }
  }
}
```

Если для локали нет записи, используются английские `title` и `subtitle` из `paymentInfoMap` в [Шаге 1](#шаг-1-настройте-платёжный-провайдер), а способ оплаты без подписи отрисовывается одним названием. Собственное название ЮKassa не меняется от локали к локали, за исключением русской, где это `ЮKassa`. Подпись переводится наравне с любой другой строкой, поэтому в русской локали она читается как `Карты, СБП`.

---

## Полный пример кода интеграции в витрину

Полный код витрины лежит в [`examples/payment-yookassa/apps/storefront`](https://github.com/gorgojs/medusa-integrations/tree/main/examples/payment-yookassa/apps/storefront), собранной на основе [Medusa DTC Starter от Gorgo](https://github.com/gorgojs/medusa-dtc-starter). В [README](https://github.com/gorgojs/medusa-integrations/blob/main/examples/payment-yookassa/README.md#what-the-example-adds-to-the-starter) примера перечислены все десять файлов, из которых состоит интеграция, включая бэкенд.
