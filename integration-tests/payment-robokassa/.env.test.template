DB_USERNAME=postgres
DB_PASSWORD=postgres
CI=true
LOG_LEVEL=error
# Required by @gorgo/medusa-integration to encrypt secret options at rest (any non-empty string).
INTEGRATION_ENCRYPTION_KEY=supersecret   # don't change this

# Robokassa configuration (fake credentials for unit/integration tests — safe to commit)
ROBOKASSA_MERCHANT_LOGIN=test-merchant     # don't change this
ROBOKASSA_HASH_ALGORITHM=md5              # don't change this
ROBOKASSA_PASSWORD_1=test-password1       # don't change this
ROBOKASSA_PASSWORD_2=test-password2       # don't change this

# Live Robokassa sandbox credentials (needed ONLY for contract tests)
CI_ROBOKASSA_MERCHANT_LOGIN=              # change this to run contract tests
CI_ROBOKASSA_HASH_ALGORITHM=              # change this to run contract tests
CI_ROBOKASSA_TEST_PASSWORD_1=             # change this to run contract tests
CI_ROBOKASSA_TEST_PASSWORD_2=             # change this to run contract tests
