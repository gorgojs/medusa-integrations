DB_USERNAME=postgres
DB_PASSWORD=postgres
CI=true
LOG_LEVEL=error
# Required by @gorgo/medusa-integration to encrypt secret options at rest (any non-empty string).
INTEGRATION_ENCRYPTION_KEY=supersecret   # don't change this

# T-Kassa configuration
TKASSA_TERMINAL_KEY=test-terminal   # don't change this
TKASSA_PASSWORD=test-password       # don't change this
CI_TKASSA_TERMINAL_KEY=             # change this to run contract tests
CI_TKASSA_PASSWORD=                 # change this to run contract tests
